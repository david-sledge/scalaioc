`namespace|scalaioc`

"say hello" `#=>` ((out: java.io.StringWriter) => out.write("No."))(`#$`("out"))

"scope" `#=>` "Universe"

"requestHandler" `#=` "I'll handle it"

"varTest" `#=>` `#$`("var")

"testVar" `#=>` `#let`("var" -> "textText", `#ref`("varTest"))

"promotion" `#=>` List(`#ref`("testVar"), `#id` = "prop")

"propTest" `#=>` `#ref`("prop")
